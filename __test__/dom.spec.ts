import { EmailsInput } from '../src/app'

describe('Test #DOM manipulation', () => {
  describe('EmailsInput', () => {
    describe('constructor', () => {
      it.todo('Create instance without `new` keyword')

      it('Create instance with `new` keyword', () => {
        const instance = new EmailsInput(document.body)

        expect(instance).toBeInstanceOf(EmailsInput)
      })
    })
  })

  describe('Email', () => {
    describe('constructor', () => {
      it.todo('empty options')
      it.todo('not empty options')
    })
  })
})
